# Thread example chunking files and sending messages to manage those files

Demonstrates managing files in threads

## Usage

Make sure the paths to the input files are correct, then run the main method
